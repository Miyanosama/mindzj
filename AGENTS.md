# Agent 工作约定

- 每次完成代码或文档更改后，都要将本次相关文件提交到 Git，并在交付时说明提交结果。
- 每次更改后都要从干净状态重新构建供测试的桌面程序：先构建前端资源，再清理 Rust 构建产物并重新编译 Tauri 应用。
- Windows 测试程序必须生成在仓库根目录的 `target/debug/` 下（通常为 `target/debug/mindzj.exe`）。构建完成后确认文件存在并报告路径。
- 提交时只暂存本次任务涉及的文件；不要把工作区中其他已有改动一并提交。
